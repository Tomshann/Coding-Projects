DeckTest

testSize(): Verifies the size of the deck after adding and removing cards. Ensures accurate count.
testGetTopCard(): Verifies if the top card of the deck is retrieved accurately.
testAddToBottom(): Checks if a card is successfully added to the bottom of the deck.
testWriteToFile(): Validates the functionality of writing card values to a file.

CardTest

testGetValue(): Ensures that the value returned by getValue() matches the face value of the card.